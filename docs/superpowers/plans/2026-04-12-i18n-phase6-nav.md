# i18n Phase 6 — Sidebar / Bottom Nav Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 web `app-sidebar.tsx` 和 mobile `app_shell.dart` 的 nav 字串搬到 i18n canonical（新增 top-level `nav.*` namespace，共 6 個 key）。統一中文對齊 mobile 的「行動 / 日誌 / 卡片 / 設定」。

**Architecture:** 同 Phase 4/5 節奏：seed canonical → `npm run i18n:sync` → `flutter gen-l10n` → web 1 檔案 / mobile 1 檔案 → 最終驗證。

**Tech Stack:** next-intl v4、Flutter gen-l10n、`AppL10n`、`useTranslations`。

**Spec：** `docs/superpowers/specs/2026-04-12-i18n-phase6-nav-design.md`

---

## 檔案結構

### 修改

**Canonical：**
- `i18n/canonical/zh-TW.json` — 加 top-level `nav` namespace
- `i18n/canonical/en.json` — 加對應英文翻譯
- `i18n/canonical/ja.json` — 加對應日文翻譯

**生成：**
- `i18n/.i18n-cache.json`
- `src/messages/{zh-TW,en,ja}.json`
- `mobile/lib/l10n/app_{zh,en,ja}.arb`
- `mobile/lib/l10n/app_localizations*.dart`

**Web：**
- `src/components/sidebar/app-sidebar.tsx` — 7 處字串（3 nav label、settings title/aria、2 aria-label）

**Mobile：**
- `mobile/lib/shell/app_shell.dart` — 4 個 NavigationDestination label

---

# Task 1: Canonical nav namespace + 翻譯 + sync

**Files:**
- Modify: `i18n/canonical/zh-TW.json`
- Modify: `i18n/canonical/en.json`
- Modify: `i18n/canonical/ja.json`
- Generated: `i18n/.i18n-cache.json`, `src/messages/*.json`, `mobile/lib/l10n/app_*.arb`

- [ ] **Step 1: 讀現有 zh-TW.json 了解結構**

```bash
head -5 i18n/canonical/zh-TW.json
```
Expected: 看到 `"common"` 和 `"settings"` top-level。Phase 5 也已加了 `"tags"`。

- [ ] **Step 2: 加 top-level `nav` namespace 到 `i18n/canonical/zh-TW.json`**

不要動 `common` / `settings` / `tags`。只在既有 namespaces 之後、檔案閉合 `}` 之前加：

```json
  "nav": {
    "tasks": "行動",
    "notes": "日誌",
    "cards": "卡片",
    "settings": "設定",
    "mainNavAria": "主導覽",
    "settingsAria": "開啟設定"
  }
```

- [ ] **Step 3: 跑 sync 產生 pending 檔**

```bash
npm run i18n:sync
```
Expected: `📊 Diff: +6 ~0 -0`, 寫出 `i18n/.pending-translations.md`, 警告 6 en + 6 ja pending。

- [ ] **Step 4: 加 en.json `nav` namespace**

在 `i18n/canonical/en.json` 的既有 namespaces 之後加：

```json
  "nav": {
    "tasks": "Tasks",
    "notes": "Notes",
    "cards": "Cards",
    "settings": "Settings",
    "mainNavAria": "Main navigation",
    "settingsAria": "Open settings"
  }
```

- [ ] **Step 5: 加 ja.json `nav` namespace**

在 `i18n/canonical/ja.json` 的既有 namespaces 之後加：

```json
  "nav": {
    "tasks": "アクション",
    "notes": "ジャーナル",
    "cards": "カード",
    "settings": "設定",
    "mainNavAria": "メインナビ",
    "settingsAria": "設定を開く"
  }
```

- [ ] **Step 6: 再跑 sync 清 pending**

```bash
npm run i18n:sync
```
Expected:
- `📊 Diff: +0 ~0 -0`
- `🗑  Removed stale ...pending-translations.md`
- `✅ Sync 完成`

- [ ] **Step 7: `i18n:check`**

```bash
npm run i18n:check
```
Expected: `✅ In sync`

- [ ] **Step 8: Commit**

```bash
git add i18n/ src/messages/ mobile/lib/l10n/app_zh.arb mobile/lib/l10n/app_en.arb mobile/lib/l10n/app_ja.arb
git commit -m "feat(i18n): 加 nav namespace canonical 和 en/ja 翻譯"
```

---

# Task 2: 重跑 flutter gen-l10n 產出新的 AppL10n getter

**Files:**
- Generated: `mobile/lib/l10n/app_localizations.dart`
- Generated: `mobile/lib/l10n/app_localizations_zh.dart`
- Generated: `mobile/lib/l10n/app_localizations_en.dart`
- Generated: `mobile/lib/l10n/app_localizations_ja.dart`

- [ ] **Step 1: 跑 gen-l10n**

```bash
cd mobile && flutter gen-l10n && cd ..
```
Expected: 無錯。

- [ ] **Step 2: 驗證新 getter 存在**

```bash
grep -cE "navTasks|navNotes|navCards|navSettings|navMainNavAria|navSettingsAria" mobile/lib/l10n/app_localizations.dart
```
Expected: ≥ `6`

- [ ] **Step 3: Analyze 生成檔**

```bash
cd mobile && flutter analyze lib/l10n/ && cd ..
```
Expected: `No issues found!`

- [ ] **Step 4: Commit**

```bash
git add mobile/lib/l10n/app_localizations*.dart
git commit -m "feat(mobile): 重跑 gen-l10n 產出新 AppL10n nav getter"
```

---

# Task 3: Web — 遷移 `app-sidebar.tsx`

**Files:**
- Modify: `src/components/sidebar/app-sidebar.tsx`

目標：7 處字串遷移。其中最複雜的是 `navItems` 常數需要重構成「存 key、render 時查」的 pattern（因為 `navItems` 是 module-level const，`useTranslations` 是 hook 不能在 module scope 叫）。

- [ ] **Step 1: 讀現有檔**

```bash
wc -l src/components/sidebar/app-sidebar.tsx
```
Expected: 117。Read 全檔了解結構。

- [ ] **Step 2: 加 import**

找到 line 1-6：
```tsx
"use client";

import { useState } from "react";
import { Link, usePathname } from "@/i18n/routing";
import { CheckSquare, NotebookPen, BookOpen, Settings } from "lucide-react";
import { SettingsModal } from "@/components/settings/settings-modal";
```

在 `@/i18n/routing` 行之後加一行 `useTranslations`：

```tsx
"use client";

import { useState } from "react";
import { useTranslations } from "next-intl";
import { Link, usePathname } from "@/i18n/routing";
import { CheckSquare, NotebookPen, BookOpen, Settings } from "lucide-react";
import { SettingsModal } from "@/components/settings/settings-modal";
```

- [ ] **Step 3: 重構 `navItems` 把 label 改成 key**

找到 line 11-30：
```tsx
// 注意：Tasks 連到 / —— `src/app/page.tsx` 是 server component，會 redirect
// 到當天的日期。這樣 sidebar 不需要在 client 端呼叫 new Date()，避免 SSR/
// hydrate 時差造成的 mismatch。
const navItems = [
  {
    href: "/",
    match: "/day/",
    icon: CheckSquare,
    label: "Tasks",
  },
  {
    href: "/notes",
    match: "/notes",
    icon: NotebookPen,
    label: "Notes",
  },
  {
    href: "/cards",
    match: "/cards",
    icon: BookOpen,
    label: "Cards",
  },
];
```

改成：
```tsx
// 注意：Tasks 連到 / —— `src/app/page.tsx` 是 server component，會 redirect
// 到當天的日期。這樣 sidebar 不需要在 client 端呼叫 new Date()，避免 SSR/
// hydrate 時差造成的 mismatch。
const navItems: {
  href: string;
  match: string;
  icon: typeof CheckSquare;
  labelKey: "tasks" | "notes" | "cards";
}[] = [
  {
    href: "/",
    match: "/day/",
    icon: CheckSquare,
    labelKey: "tasks",
  },
  {
    href: "/notes",
    match: "/notes",
    icon: NotebookPen,
    labelKey: "notes",
  },
  {
    href: "/cards",
    match: "/cards",
    icon: BookOpen,
    labelKey: "cards",
  },
];
```

- [ ] **Step 4: 改 `NavLink` 的 props 從 `label: string` 改成也接受 string**

`NavLink` 還是接 `label: string`，**不改 prop 型別**，只是改呼叫端傳進去的值。這步不動 `NavLink` 本身。確認 line 32-57 的 `NavLink` 定義保持原樣：
```tsx
function NavLink({
  href,
  active,
  icon: Icon,
  label,
}: {
  href: string;
  active: boolean;
  icon: typeof CheckSquare;
  label: string;
}) {
  ...
}
```

- [ ] **Step 5: 遷移 `SettingsButton`**

找到 line 59-70：
```tsx
function SettingsButton({ onClick }: { onClick: () => void }) {
  return (
    <button
      onClick={onClick}
      title="設定"
      aria-label="開啟設定"
      className="flex items-center justify-center w-11 h-11 rounded-lg text-text-dim hover:text-foreground hover:bg-border/50 transition-colors"
    >
      <Settings className="h-5 w-5" />
    </button>
  );
}
```

改成把 label 改由 props 傳入（避免在子 component 內再叫一次 hook）：
```tsx
function SettingsButton({
  onClick,
  title,
  ariaLabel,
}: {
  onClick: () => void;
  title: string;
  ariaLabel: string;
}) {
  return (
    <button
      onClick={onClick}
      title={title}
      aria-label={ariaLabel}
      className="flex items-center justify-center w-11 h-11 rounded-lg text-text-dim hover:text-foreground hover:bg-border/50 transition-colors"
    >
      <Settings className="h-5 w-5" />
    </button>
  );
}
```

- [ ] **Step 6: 在 `AppSidebar` 頂部叫 `useTranslations`，並把 label 傳進去**

找到 line 72-116（整個 `AppSidebar`）：
```tsx
export function AppSidebar() {
  const pathname = usePathname();
  const [settingsOpen, setSettingsOpen] = useState(false);

  return (
    <>
      {/* Desktop: left sidebar */}
      <aside
        aria-label="主導覽"
        className="hidden md:flex fixed left-0 top-0 bottom-0 z-40 w-14 flex-col items-center gap-2 border-r border-border bg-background pt-4 pb-16"
      >
        {navItems.map((item) => (
          <NavLink
            key={item.match}
            href={item.href}
            active={pathname.startsWith(item.match)}
            icon={item.icon}
            label={item.label}
          />
        ))}
        <div className="mt-auto">
          <SettingsButton onClick={() => setSettingsOpen(true)} />
        </div>
      </aside>

      {/* Mobile: bottom bar */}
      <nav
        aria-label="主導覽"
        className="md:hidden fixed bottom-0 left-0 right-0 z-40 flex items-center justify-around h-14 border-t border-border bg-background"
      >
        {navItems.map((item) => (
          <NavLink
            key={item.match}
            href={item.href}
            active={pathname.startsWith(item.match)}
            icon={item.icon}
            label={item.label}
          />
        ))}
        <SettingsButton onClick={() => setSettingsOpen(true)} />
      </nav>

      <SettingsModal open={settingsOpen} onOpenChange={setSettingsOpen} />
    </>
  );
}
```

改成：
```tsx
export function AppSidebar() {
  const t = useTranslations("nav");
  const pathname = usePathname();
  const [settingsOpen, setSettingsOpen] = useState(false);

  return (
    <>
      {/* Desktop: left sidebar */}
      <aside
        aria-label={t("mainNavAria")}
        className="hidden md:flex fixed left-0 top-0 bottom-0 z-40 w-14 flex-col items-center gap-2 border-r border-border bg-background pt-4 pb-16"
      >
        {navItems.map((item) => (
          <NavLink
            key={item.match}
            href={item.href}
            active={pathname.startsWith(item.match)}
            icon={item.icon}
            label={t(item.labelKey)}
          />
        ))}
        <div className="mt-auto">
          <SettingsButton
            onClick={() => setSettingsOpen(true)}
            title={t("settings")}
            ariaLabel={t("settingsAria")}
          />
        </div>
      </aside>

      {/* Mobile: bottom bar */}
      <nav
        aria-label={t("mainNavAria")}
        className="md:hidden fixed bottom-0 left-0 right-0 z-40 flex items-center justify-around h-14 border-t border-border bg-background"
      >
        {navItems.map((item) => (
          <NavLink
            key={item.match}
            href={item.href}
            active={pathname.startsWith(item.match)}
            icon={item.icon}
            label={t(item.labelKey)}
          />
        ))}
        <SettingsButton
          onClick={() => setSettingsOpen(true)}
          title={t("settings")}
          ariaLabel={t("settingsAria")}
        />
      </nav>

      <SettingsModal open={settingsOpen} onOpenChange={setSettingsOpen} />
    </>
  );
}
```

- [ ] **Step 7: Build 驗證**

```bash
npx next build 2>&1 | tail -15
```
Expected: `✓ Compiled successfully`, no TypeScript error.

- [ ] **Step 8: Commit**

```bash
git add src/components/sidebar/app-sidebar.tsx
git commit -m "refactor(web): app-sidebar nav 字串改用 useTranslations"
```

---

# Task 4: Mobile — 遷移 `app_shell.dart`

**Files:**
- Modify: `mobile/lib/shell/app_shell.dart`

目標：4 個 NavigationDestination label 改用 `AppL10n`。

- [ ] **Step 1: 讀現有檔**

```bash
cat mobile/lib/shell/app_shell.dart
```
Expected: 49 行的 `AppShell` StatelessWidget。

- [ ] **Step 2: 加 import**

找到 line 1-2：
```dart
import 'package:flutter/material.dart';
import 'package:lucide_icons/lucide_icons.dart';
```

在 `lucide_icons` 之後加：
```dart
import 'package:flutter/material.dart';
import 'package:lucide_icons/lucide_icons.dart';
import '../l10n/app_localizations.dart';
```

- [ ] **Step 3: 在 build method 加 `l` + 把 4 個 label 遷移**

找到 line 17-47：
```dart
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: child,
      bottomNavigationBar: NavigationBar(
        selectedIndex: currentIndex,
        onDestinationSelected: onTabChanged,
        destinations: [
          NavigationDestination(
            icon: Icon(LucideIcons.checkSquare),
            selectedIcon: Icon(LucideIcons.checkSquare),
            label: '行動',
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.fileEdit),
            selectedIcon: Icon(LucideIcons.fileEdit),
            label: '日誌',
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.bookOpen),
            selectedIcon: Icon(LucideIcons.bookOpen),
            label: '卡片',
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.settings),
            selectedIcon: Icon(LucideIcons.settings),
            label: '設定',
          ),
        ],
      ),
    );
  }
```

改成：
```dart
  @override
  Widget build(BuildContext context) {
    final l = AppL10n.of(context)!;
    return Scaffold(
      body: child,
      bottomNavigationBar: NavigationBar(
        selectedIndex: currentIndex,
        onDestinationSelected: onTabChanged,
        destinations: [
          NavigationDestination(
            icon: Icon(LucideIcons.checkSquare),
            selectedIcon: Icon(LucideIcons.checkSquare),
            label: l.navTasks,
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.fileEdit),
            selectedIcon: Icon(LucideIcons.fileEdit),
            label: l.navNotes,
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.bookOpen),
            selectedIcon: Icon(LucideIcons.bookOpen),
            label: l.navCards,
          ),
          NavigationDestination(
            icon: Icon(LucideIcons.settings),
            selectedIcon: Icon(LucideIcons.settings),
            label: l.navSettings,
          ),
        ],
      ),
    );
  }
```

- [ ] **Step 4: Analyze**

```bash
cd mobile && flutter analyze lib/shell/app_shell.dart && cd ..
```
Expected: `No issues found!`

- [ ] **Step 5: 跑 locale_provider test 確認沒牽連**

```bash
cd mobile && flutter test test/core/locale_provider_test.dart && cd ..
```
Expected: `All tests passed!` (6/6)

- [ ] **Step 6: Commit**

```bash
git add mobile/lib/shell/app_shell.dart
git commit -m "refactor(mobile): app_shell nav 字串改用 AppL10n"
```

---

# Task 5: 最終驗證 + QA checklist

**Files:** 無（純驗證 + 印 checklist）

- [ ] **Step 1: `i18n:check`**

```bash
npm run i18n:check
```
Expected: `✅ In sync`

- [ ] **Step 2: Web build**

```bash
npx next build 2>&1 | tail -15
```
Expected: `✓ Compiled successfully`

- [ ] **Step 3: Mobile analyze 全檔**

```bash
cd mobile && flutter analyze 2>&1 | tail -5 && cd ..
```
Expected: `No issues found!`

- [ ] **Step 4: Mobile tests**

```bash
cd mobile && flutter test test/core/locale_provider_test.dart 2>&1 | tail -5 && cd ..
```
Expected: `All tests passed!` (6/6)

- [ ] **Step 5: Git log 收尾**

```bash
git log --oneline -n 6
```
Expected: 看到 Phase 6 新加的 4 個 refactor/feat commit。

- [ ] **Step 6: 印手動 QA checklist**

```
========================================================
Phase 6 手動 QA checklist
========================================================

Web (npm run dev):
  1. 登入到 /zh-TW/day/<today>
  2. 看 sidebar 左邊 icon bar：
     a. Tooltip hover 每個 icon 應為「行動 / 日誌 / 卡片」
     b. 設定 icon tooltip「設定」
     c. <aside> 的 aria-label 應為「主導覽」（DevTools Accessibility panel 確認）
  3. 切 /en/... 看 sidebar：
     a. Tooltip 變 "Tasks / Notes / Cards / Settings"
     b. aria-label 變 "Main navigation" / "Open settings"
  4. 切 /ja/... 看 sidebar：
     a. Tooltip 變「アクション / ジャーナル / カード / 設定」
     b. aria-label 變「メインナビ」
  5. Mobile viewport（<768px）：底部 nav bar 顯示，切語言後 tooltip 跟著變

Mobile (cd mobile && flutter run):
  1. 登入，看到 BottomNavigationBar 4 個 tab：
     a. 中文：行動 / 日誌 / 卡片 / 設定
  2. 進設定，切 EN：
     a. 立刻變：Tasks / Notes / Cards / Settings
  3. 切日文：
     a. アクション / ジャーナル / カード / 設定
  4. 切自動：回系統語言
========================================================
```

- [ ] **Step 7: Commit 任何 lint 調整（若有）**

```bash
git status
git add -A
git commit -m "chore(i18n): Phase 6 最終驗證" || echo "nothing to commit"
```

---

## 完成條件

- ✅ `i18n/canonical/{zh-TW,en,ja}.json` 含 top-level `nav.*` 6 key
- ✅ `npm run i18n:check` 回 `✅ In sync`
- ✅ Web `app-sidebar.tsx` 無硬字串（code comment 除外）
- ✅ Mobile `app_shell.dart` 無硬字串（code comment 除外）
- ✅ `npx next build` 通過、`flutter analyze` 無 issue
- ✅ 既有 locale_provider test 仍 6/6

## 已知限制

- Feature 頁面本體字串（tasks / cards / notes / day view / login / card-detail）仍為中文，等後續 phase
